import { StyleSheet } from 'react-native';

export const Style = StyleSheet.create({
    txt: {
        marginTop: 10,
        marginBottom: 20,
        borderWidth: 0.5,
        borderRadius: 3,
        padding: 1

    }
});