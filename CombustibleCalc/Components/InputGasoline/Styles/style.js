import { StyleSheet } from 'react-native';

export const Style = StyleSheet.create({
    txt: {
        marginBottom: 20,
        marginTop: 10,
        padding: 1,
        borderRadius: 3,
        borderWidth: 0.5,
    }
});